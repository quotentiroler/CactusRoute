[project]
name = "cactus-route"
version = "0.1.0"
description = "Adaptive edge/cloud hybrid router for FunctionGemma hackathon"
requires-python = ">=3.11"
dependencies = [
    "google-genai>=1.0",
    "requests>=2.28",
    "rich>=13.0",
]
